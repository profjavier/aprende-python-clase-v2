- Creamos un proyecto que muestra tres paneles en un QVBoxLayout (v01)

---------------------
import sys

from PyQt6.QtWidgets import QWidget, QApplication, QVBoxLayout, QStackedWidget


class ArticulosApp(QWidget):
    def __init__(self):
        super().__init__()
        self.articulos = []
        self.config_ui()

    def config_ui(self):
        self.setWindowTitle("Menu botones - Entradas")
        self.resize(600, 500)

        # Configuracion layouts
        layout = QVBoxLayout(self)
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setSpacing(0)

        self.panelMenu = QWidget(self)
        layout.addWidget(self.panelMenu)
        self.panelMenu.setStyleSheet("background-color: #AA0000;")
        self.panelMenu.setFixedHeight(50)

        self.panelCentral = QStackedWidget(self)
        layout.addWidget(self.panelCentral)
        self.panelCentral.setStyleSheet("background-color: #00AAAA;")

        self.panelStatus = QWidget(self)
        layout.addWidget(self.panelStatus)
        self.panelStatus.setStyleSheet("background-color: #00AA00;")
        self.panelStatus.setFixedHeight(50)


if __name__ == "__main__":
    app = QApplication(sys.argv)
    ventana = ArticulosApp()
    ventana.show()
    sys.exit(app.exec())
---------------------

    Vemos una separación entre paneles y con la ventana. para quitarlos ponemos
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setSpacing(0)

- Configuramos el panel menu (v02)
    Ponemos los botones y probamos que funcionan

- Configuramos el panel central (v03)
    Creamos los paneles de nuevo... y configuramos los cambios de panel

- Configuramos el panel nuevo (v04)
    Creamos los widgets del panel nuevo...
    Creamos la funcion para guardar (sin guardar) y probamos el boton

- Configuramos el panel nuevo (v05)
    Reconfiguramos la aplicacion: Creamos una clase para la opcion Nuevo y recolocamos el código (ui_nuevo_articulo)
    Creamos un módulo con las clases de componentes personalizados (ui_components)

- Establecemos la configuracion para que guarde los articulos en un fichero (v06)
    Creamos una clase ArticulosDao que gestionará el fichero articulos
    Creamamos una instancia de ArticulosDao en la app y la pasamos como argumento a UINUevoArticulo